{
  "name": "yieldmind",
  "version": "1.0.0",
  "description": "YieldMind - AI-Powered DeFi Yield Optimizer on BNB Chain",
  "private": true,
  "workspaces-disabled": [
    "contracts",
    "frontend",
    "backend"
  ],
  "scripts": {
    "compile": "cd contracts && npx hardhat compile",
    "test": "cd contracts && npx hardhat test",
    "deploy:local": "cd contracts && npx hardhat run scripts/deploy.js --network localhost",
    "deploy:testnet": "cd contracts && npx hardhat run scripts/deploy.js --network bscTestnet",
    "node": "cd contracts && npx hardhat node",
    "frontend:dev": "cd frontend && npm run dev",
    "backend:dev": "cd backend && npm run dev",
    "audit": "npm audit"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "dependencies": {
    "@nomicfoundation/hardhat-toolbox": "^7.0.0",
    "@openzeppelin/contracts": "^5.6.1",
    "chai": "^4.5.0",
    "ethers": "^6.16.0",
    "hardhat": "^3.7.0",
    "resolve": "^1.22.12"
  }
}
